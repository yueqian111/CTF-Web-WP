# Bugku Web 题 alert WriteUp

## 题目信息

- 题目：alert

- 分数：10

- 难度：入门

- 考点：HTML实体编码、网页源代码查看

---

## 解题思路

页面通过大量 `alert` 弹窗干扰操作，flag 被隐藏在网页源代码底部的 HTML 实体编码中，只需提取并解码即可得到答案。

---

## 解题步骤

### 1. 进入题目场景

点击「启动场景」进入题目页面，会发现页面弹出大量 `alert("来找找吧")` 和 `alert("flag就在这里")` 弹窗，干扰正常操作。

### 2. 查看网页源代码

- 方式一：右键页面 → 选择「查看网页源代码」

- 方式二：按 `F12` 打开开发者工具，切换到「Elements」或「Sources」面板查看代码

在源代码中，会看到大量重复的 `alert` 语句，滚动到页面最底部，即可找到被隐藏的 HTML 实体编码字符串：

```HTML

&#102;&#108;&#97;&#103;&#123;&#55;&#57;&#98;&#56;&#55;&#53;&#53;&#99;&#53;&#99;&#56;&#55;&#102;&#56;&#54;&#51;&#98;&#101;&#99;&#53;&#55;&#52;&#99;&#98;&#51;&#48;&#100;&#99;&#97;&#57;&#125;
```

#### 3：使用工具解码

- 在线工具：如 [HTML Entity Decoder](https://www.functions-online.com/html_entity_decode.html)

- Python 脚本：

    ```Python
    
    import html
    encoded = "&#102;&#108;&#97;&#103;&#123;&#55;&#57;&#98;&#56;&#55;&#53;&#53;&#99;&#53;&#99;&#56;&#55;&#102;&#56;&#54;&#51;&#98;&#101;&#99;&#53;&#55;&#52;&#99;&#98;&#51;&#48;&#100;&#99;&#97;&#57;&#125;"
    flag = html.unescape(encoded)
    print(flag)
    ```

### 4. 提交 flag

将解码得到的 `flag{79b8755c5c87f863bec574cb30dca9}` 输入到题目提交框中，即可完成解题。
> （注：文档部分内容可能由 AI 生成）